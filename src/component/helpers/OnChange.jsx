export function OnChange(setfileUrl) {
  const onChangeFeedbacks = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeDetails = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeSlider = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeLocation = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangePartner = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeStep = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeStepCategory = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeTools = async (e) => {
    setfileUrl(e.target.files[0]);
  };

  //about
  const onChangeAboutHome = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeAboutSuspention = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeAboutExperiance = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeAboutVehicle = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeAboutProfisson = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeAboutSliders = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeAboutVideo = async (e) => {
    setfileUrl(e.target.files[0]);
  };

  //sercice
  const onChangeService = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeServiceAutoRepairs = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeServiceRepairs = async (e) => {
    setfileUrl(e.target.files[0]);
  };

  //news
  const onChangenewsHeaders = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangenewsCategory = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeonNewsMains = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangeonNewsSlider = async (e) => {
    setfileUrl(e.target.files[0]);
  };

  //contact
  const onChangContactHeader = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  const onChangContactInfo = async (e) => {
    setfileUrl(e.target.files[0]);
  };

  //site
  const onChangesitemasterPageInfoes = async (e) => {
    setfileUrl(e.target.files[0]);
  };
  return {
    onChangeFeedbacks,
    onChangeDetails,
    onChangeLocation,
    onChangePartner,
    onChangeSlider,
    onChangeStep,
    onChangeStepCategory,
    onChangeTools,
    onChangeAboutHome,
    onChangeAboutSuspention,
    onChangeAboutExperiance,
    onChangeAboutVehicle,
    onChangeAboutProfisson,
    onChangeAboutSliders,
    onChangeAboutVideo,
    onChangeService,
    onChangeServiceAutoRepairs,
    onChangeServiceRepairs,
    onChangenewsHeaders,
    onChangenewsCategory,
    onChangeonNewsMains,
    onChangeonNewsSlider,
    onChangContactHeader,
    onChangContactInfo,
    onChangesitemasterPageInfoes,
  };
}
